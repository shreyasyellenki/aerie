{
  "name": "action-server",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "build": "npm run lint; npm run build-quiet",
    "build-quiet": "tsc -p tsconfig.json",
    "clean": "rm -rf build",
    "start": "npm run clean; npm run build-quiet; node build/app.js",
    "dev": "npm run clean; npm run build; nodemon --watch  src/**/*  -e ts --exec 'node --inspect=0.0.0.0:9229 --require ts-node/register build/app.js'",
    "test": "node -r ts-node/register tests/*.test.ts",
    "lint": "eslint src/**/*.ts",
    "reformat": "npx prettier --config ./.prettierrc.json -w ./src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Action server for sequencing",
  "devDependencies": {
    "@tsconfig/node22": "^22.0.0",
    "@types/express": "^5.0.0",
    "@types/pg": "^8.11.11",
    "eslint-config-sheriff": "^25.8.0",
    "nodemon": "^3.1.9",
    "prettier": "^3.4.2",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@nasa-jpl/aerie-actions": "0.1.8",
    "express": "^5.0.1",
    "pg": "^8.13.3",
    "piscina": "4.9.2",
    "winston": "^3.17.0"
  }
}
